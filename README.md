# ctrwatch
A small Go CLI/TUI for watching local containers through the Docker Engine API. It can list running containers, inspect metadata, stream logs from multiple containers, and show basic stats.
